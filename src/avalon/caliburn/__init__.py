"""Caliburn — featherweight Blade-familiar view engine (M5).

Templates use the ``.cal.html`` extension. Inline code uses
``@python`` / ``@endpython`` only — no freeform Python embedding.
"""

__all__: list[str] = []
